<form method="post" action="{{ path('app_ejemplo_delete', {'id': ejemplo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ejemplo.id) }}">
    <button class="btn">Delete</button>
</form>
